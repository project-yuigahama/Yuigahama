const { Event, KlasaUser } = require('klasa')
const { MessageReaction } = require('discord.js')

class MessageReactionRemove extends Event {
  async run (...params) {
  // TODO: pushpinが削除された時の処理を書く
  }
}
